# handly regex

```js
const searchTerm = 'example';
const regex = new RegExp(searchTerm, "i");
/* regular expression object using the RegExp constructor with the i flag. The i flag stands for "ignore case," which means the regular expression will perform a case-insensitive search. */
```
