// Write your code here:
const greetAliens = (arr) => {
    return arr.forEach(each => console.log(`Oh powerful ${each}, we humans offer our unconditional surrender!`))
    }
  
  
  
  // When you're ready to test your code, uncomment the below and run:
  
  const aliens = ["Blorgous", "Glamyx", "Wegord", "SpaceKing"];
  // aliens.forEach(each => console.log(`Oh powerful ${each}, we humans offer our unconditional surrender!`))
  greetAliens(aliens);
  
  